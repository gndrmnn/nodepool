- hosts: all
  roles:
    - run-devstack
    - role: bindep
      bindep_profile: default

  tasks:
    - name: Ensure nodepool services directories
      file:
        path: '{{ ansible_user_dir }}/work/{{ item }}'
        state: directory
      with_items:
        - logs/nodepool
        - etc
        - images
