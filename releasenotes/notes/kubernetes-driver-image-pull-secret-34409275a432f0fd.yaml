---
features:
  - The pool configuration for the Kubernetes driver now accepts an `imagePullSecrets` attribute
    that can specify the Pull Secret to be used for the pod. This is mostly useful when pulling
    images from private repositories.
