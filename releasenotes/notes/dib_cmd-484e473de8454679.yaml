---
features:
  - |
    A ``diskimage`` can specify the full path to the diskimage-builder
    command with the ``dib_cmd`` configuration parameter.  CI has replaced
    the ``--fake`` parameter from  ``nodepool-builder`` with explicit calls
    in testing fixtures.
