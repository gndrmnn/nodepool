---
features:
  - |
    Provider labels for the OpenStack driver are now able to toggle
    :attr:`providers.[openstack].pools.labels.host-key-checking`. This
    overrides any networks defined by
    :attr:`providers.[openstack].pools.networks`.
