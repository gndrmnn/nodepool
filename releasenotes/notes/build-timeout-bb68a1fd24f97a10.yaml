---
features:
  - |
    A new option (--build-timeout) has been added to the builder process
    to control how long the builder should wait for image builds before
    giving up. The default is 8 hours.
