zookeeper-servers:
  - host: {zookeeper_host}
    port: {zookeeper_port}
    chroot: {zookeeper_chroot}

zookeeper-auth:
  scheme: sasl
  username: super
  password: adminsecret

diskimages:
  - name: fake-image
    env-vars:
      REG_PASSWORD: secret
